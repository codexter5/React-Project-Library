This is react project for library system,
where a person can browse books, and can add 
or delete their own book to the system.
Here the backend is done using the json
local server.
Command used to start the project.
1) npm run dev // This is used to run the react using vite.
2) npm run server // This is used to use the local json server data.
